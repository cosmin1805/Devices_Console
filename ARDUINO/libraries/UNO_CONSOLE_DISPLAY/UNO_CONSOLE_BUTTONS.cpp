#include <UNO_CONSOLE_DISPLAY.h>

void display::buttonsInit()
{
    int i =0,x_pos1,y_pos1,w,h,count;
    for (int16_t x = 0; x <= w * (count - 1); x += w) {
        nav_bar[i]={x,y_pos1,w,x+}
    i++;
  }
}

